# Experiment 3: Pair plot and Correlation Matrix

from google.colab import files
uploaded = files.upload()  # upload your dataset e.g. 'iris.csv' or 'california_housing.csv'

import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt

df = pd.read_csv('dataset.csv') # replace with your file name

print(df.head())
print(df.shape)
print(df.info())
print(df.describe())

# 1. Pair Plot
sns.pairplot(df)
plt.suptitle("Pair Plot of Variables", y=1.02)
plt.show()

# 2. Correlation Matrix
corr_matrix = df.corr(numeric_only=True)
print(corr_matrix)

# 3. Heatmap of Correlation Matrix
plt.figure(figsize=(8,6))
sns.heatmap(corr_matrix, annot=True, cmap='coolwarm', linewidths=0.5)
plt.title("Correlation Matrix Heatmap")
plt.show()

# 4. Individual scatter plots (optional)
# sns.scatterplot(x=df['column1'], y=df['column2'])
# plt.show()
